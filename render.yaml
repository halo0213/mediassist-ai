services:
  - type: web
    name: mediassist-ai
    env: python
    buildCommand: pip install -r mcp_server/requirements.txt
    startCommand: cd mcp_server && uvicorn server:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: GEMINI_API_KEY
        sync: false